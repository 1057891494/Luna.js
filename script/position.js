(function(window, Luna, undefined) {
    'use strict';

    Luna.prototype.extend({

        /*计算元素位置*/
        "position": function() {
            var $$this = Luna(this);
            throw new Error('此方法开发中。。。。。。');
        }
    });
})(window, window.Luna);
